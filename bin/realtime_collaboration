#!/usr/bin/env ruby
# frozen_string_literal: true

$LOAD_PATH.unshift(File.expand_path('../lib', __dir__))

require 'rackup'
require 'realtime_collaboration'

Rackup::Server.start(
  app: RealtimeCollaboration.app,
  server: 'puma',
  Host: '0.0.0.0',
  Port: Integer(ENV.fetch('PORT', '9393'))
)
